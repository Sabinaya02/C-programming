// Online C compiler to run C program online
#include <stdio.h>
#include<string.h>
int main() {
    char str[50];
    char str1[50];
    scanf("%s",str);
    scanf("%s",str1);
    strcat(str,str1);=>strncat(str,str1,3);---to print particular words
    printf("%s",str);
}
    
