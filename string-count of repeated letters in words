#include<stdio.h>
#include<string.h>
int main(){
    char str[50];
    scanf("%s\n",str);
    char c;
    scanf("%c",&c);
    int l=strlen(str);
    int count=0;
    for(int i=0;i<l;i++){
        if(str[i]==c){
            count++;
        }
    }
    printf("%d",count);
}
