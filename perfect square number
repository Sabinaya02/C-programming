// Online C compiler to run C program online
#include <stdio.h>
#include<math.h>
int main() {
int a;
scanf("%d",&a);
int x=sqrt(a);
if(x*x==a){
    printf("Yes");
}else{
    printf("No");
}
    return 0;
}
